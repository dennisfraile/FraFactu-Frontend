# F5.3 — Nota de Crédito (05) + Nota de Débito (06) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Emitir Notas de Crédito (DTE 05) y Notas de Débito (DTE 06) que ajustan un CCF (03) ya emitido, referenciándolo como documento relacionado, en estado PENDIENTE (`guardar-pendiente`).

**Architecture:** Dos estrategias nuevas (`nc05`, `nd06`) en el registro por tipo de DTE, reutilizando el cálculo IVA-neto del CCF (extraído a un helper compartido). Un paso de "documento original" (`DocumentoOriginalSection`) busca el CCF vía endpoints dedicados del backend y, para NC, precarga sus ítems; para ND, el cuerpo arranca vacío. El receptor se hereda del original. Se emite como PENDIENTE con `documentosRelacionados`.

**Tech Stack:** React + TypeScript + Vite, Vitest + Testing Library + MSW, Zod, React Query, Tailwind (design-system propio).

## Global Constraints

- **Sólo CCF (03)** como documento original. No NC/ND sobre 01 ni 14 en esta rebanada.
- **NC (05):** precarga ítems del original (`detalle-para-nc`); el usuario sólo **quita líneas y reduce** cantidad/precio (no añade líneas). **ND (06):** referencia el original + ítems nuevos a mano (sin precarga).
- **Versión MH:** NC `version=4`, ND `version=4`.
- **Cálculo = CCF:** IVA neto `base×0.13`; IVA representado como tributo: cada ítem gravado `tributos:['20']`; `resumen.tributos:[{codigo:'20',descripcion:'Impuesto al Valor Agregado 13%',valor:totalIva}]`; `montoTotalOperacion = subTotal + Σtributos`. `precioIncluyeIva:false`.
- **Documento relacionado** (obligatorio ≥1 para 05/06): `{ tipoDocumento:'03', tipoGeneracion:1, numeroDocumento:<codigoGeneracion del CCF>, fechaEmision:<fecha del CCF, ISO yyyy-MM-dd> }`.
- **NC/ND no requieren caja** (`requiereCaja:false`) ni mueven stock (`descuentaStock:false`). Receptor obligatorio, **heredado del original** (con `receptorId`), no se crea ni se busca.
- **Endpoints backend** (query param se llama **`search`**, NO `searchTerm`): `GET /api/facturas/buscar-para-nc?search=…`, `GET /api/facturas/buscar-para-nd?search=…`, `GET /api/facturas/{id}/detalle-para-nc`, `GET /api/facturas/{id}/detalle-para-nd`.
- Tras CADA tarea de estrategia/test correr `npm run build` (vitest no detecta errores de `tsc`/excess-property).
- Emisión vía `POST /api/facturas/guardar-pendiente` → estado `PENDIENTE_ENVIO`. Sin transmisión a MH (difiere a F5.4).

## File Structure

- `src/features/facturacion/calc/iva-neto.ts` (nuevo) — `calcItemIvaNeto`, `calcResumenIvaNeto` (cálculo CCF/NC/ND compartido).
- `src/features/facturacion/dte/ccf03.ts` (modificar) — delega en `iva-neto`.
- `src/features/facturacion/dte/nc05.ts`, `nd06.ts` (nuevos) — estrategias.
- `src/features/facturacion/dte/registry.ts` (modificar) — registrar '05','06'.
- `src/features/facturacion/dte/strategy.ts` (modificar) — flags `requiereDocumentoRelacionado`, `prefillDesdeOriginal`; campo `documentoRelacionado` en `FacturaFormValues`.
- `src/features/facturacion/types.ts` (modificar) — `DocumentoRelacionadoDto`, `BuscarParaNcResultDto`, `DetalleParaNcDto`, `DetalleItemParaNcDto`; `documentosRelacionados?` en `CreateFacturaDto`.
- `src/features/facturacion/mappers.ts` (modificar) — incluir `documentosRelacionados`.
- `src/features/facturacion/schemas.ts` (modificar) — exigir documento relacionado para 05/06.
- `src/features/facturacion/api.ts` (modificar) — `buscarParaNc/Nd`, `detalleParaNc/Nd`.
- `src/features/facturacion/hooks.ts` (modificar) — `useBuscarParaNc/Nd`, `useDetalleParaNc/Nd`.
- `src/test/msw/facturacion-handlers.ts` (modificar) — handlers de los 4 endpoints.
- `src/features/facturacion/components/DocumentoOriginalSection.tsx` (nuevo) — buscar + seleccionar + precargar.
- `src/features/facturacion/pages/EmitirDtePage.tsx` (modificar) — estado documento relacionado + render condicional.
- `src/features/facturacion/components/DatosGeneralesSection.tsx` (modificar) — ocultar receptor cuando `requiereDocumentoRelacionado`.
- `src/features/facturacion/components/TotalesPanel.tsx`, `VistaPreviaDte.tsx` (modificar) — soportar 05/06.

---

## Task 1: Extraer cálculo IVA-neto compartido y refactorizar CCF

**Files:**
- Create: `src/features/facturacion/calc/iva-neto.ts`
- Create: `src/features/facturacion/calc/iva-neto.test.ts`
- Modify: `src/features/facturacion/dte/ccf03.ts`

**Interfaces:**
- Consumes: `round` (`../calc/round`), `ItemInput`/`ItemCalculado`/`ResumenNumerico` (`../calc/types`).
- Produces: `calcItemIvaNeto(input: ItemInput): ItemCalculado`, `calcResumenIvaNeto(items: ItemCalculado[]): ResumenNumerico` (idénticos a la lógica actual de `calcItemCcf03`/`calcResumenCcf03`).

- [ ] **Step 1: Escribir el test** `src/features/facturacion/calc/iva-neto.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { calcItemIvaNeto, calcResumenIvaNeto } from './iva-neto'

describe('calcItemIvaNeto (IVA neto)', () => {
  it('gravado: ventaGravada = base, ivaItem = base*0.13', () => {
    const r = calcItemIvaNeto({ cantidad: 2, precioUni: 50, tipoImpuesto: 1 })
    expect(r.ventaGravada).toBe(100)
    expect(r.ivaItem).toBe(13)
  })
  it('exento: sin IVA', () => {
    const r = calcItemIvaNeto({ cantidad: 1, precioUni: 80, tipoImpuesto: 2 })
    expect(r.ventaExenta).toBe(80)
    expect(r.ivaItem).toBe(0)
  })
})

describe('calcResumenIvaNeto', () => {
  it('suma gravada + IVA y arma montoTotalOperacion', () => {
    const items = [calcItemIvaNeto({ cantidad: 2, precioUni: 50, tipoImpuesto: 1 })]
    const r = calcResumenIvaNeto(items)
    expect(r.subTotal).toBe(100)
    expect(r.totalIva).toBe(13)
    expect(r.montoTotalOperacion).toBe(113)
    expect(r.totalPagar).toBe(113)
    expect(r.reteRenta).toBe(0)
  })
})
```

- [ ] **Step 2: Ejecutar (falla)** — `npx vitest run src/features/facturacion/calc/iva-neto.test.ts` → FAIL (módulo no existe).

- [ ] **Step 3: Crear `iva-neto.ts`** (copia exacta de la lógica actual de ccf03):
```ts
import { round } from './round'
import type { ItemInput, ItemCalculado, ResumenNumerico } from './types'

const TASA_IVA = 0.13

// Cálculo compartido por CCF (03), NC (05) y ND (06): precio NETO, IVA agregado (base × 0.13).
export function calcItemIvaNeto(input: ItemInput): ItemCalculado {
  const base = round(input.precioUni * input.cantidad - (input.montoDescuento ?? 0), 2)
  const r: ItemCalculado = { ventaGravada: 0, ventaExenta: 0, ventaNoSuj: 0, ivaItem: 0 }
  if (input.tipoImpuesto === 1) {
    r.ventaGravada = base
    r.ivaItem = round(base * TASA_IVA, 2)
  } else if (input.tipoImpuesto === 2) {
    r.ventaExenta = base
  } else {
    r.ventaNoSuj = base
  }
  return r
}

export function calcResumenIvaNeto(items: ItemCalculado[]): ResumenNumerico {
  const totalGravada = round(items.reduce((s, i) => s + i.ventaGravada, 0), 2)
  const totalExenta = round(items.reduce((s, i) => s + i.ventaExenta, 0), 2)
  const totalNoSuj = round(items.reduce((s, i) => s + i.ventaNoSuj, 0), 2)
  const totalIva = round(items.reduce((s, i) => s + i.ivaItem, 0), 2)
  const subTotal = round(totalGravada + totalExenta + totalNoSuj, 2)
  const montoTotalOperacion = round(subTotal + totalIva, 2)
  const totalPagar = montoTotalOperacion
  return { totalGravada, totalExenta, totalNoSuj, totalIva, subTotal, totalPagar, montoTotalOperacion, reteRenta: 0 }
}
```

- [ ] **Step 4: Refactorizar `ccf03.ts`** para delegar. Reemplaza el cuerpo de `calcItemCcf03`/`calcResumenCcf03` por re-exports delegados (mantén los nombres exportados para no romper imports):
```ts
import { calcItemIvaNeto, calcResumenIvaNeto } from '../calc/iva-neto'
// ...
export const calcItemCcf03 = calcItemIvaNeto
export const calcResumenCcf03 = calcResumenIvaNeto
```
Elimina las constantes/funciones de cálculo ya movidas (TASA_IVA local, cuerpos viejos) y el import de `round` si queda sin uso. El resto de `ccf03.ts` (estrategia, buildItemDto con `tributos`, buildResumenDto con `tributos`) NO cambia.

- [ ] **Step 5: Ejecutar** — `npx vitest run src/features/facturacion/calc/iva-neto.test.ts src/features/facturacion/dte/ccf03.test.ts src/features/facturacion/dte/ccf03.mapper.test.ts` → PASS (paridad CCF intacta).

- [ ] **Step 6: `npm run build`** → PASS.

- [ ] **Step 7: Commit**
```bash
git add src/features/facturacion/calc/iva-neto.ts src/features/facturacion/calc/iva-neto.test.ts src/features/facturacion/dte/ccf03.ts
git commit -m "refactor(f5.3): extraer cálculo IVA-neto compartido (CCF/NC/ND)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Tipos y flags de estrategia para documento relacionado

**Files:**
- Modify: `src/features/facturacion/types.ts`
- Modify: `src/features/facturacion/dte/strategy.ts`

**Interfaces:**
- Produces: `DocumentoRelacionadoDto`, `BuscarParaNcResultDto`, `DetalleParaNcDto`, `DetalleItemParaNcDto` (types.ts); `CreateFacturaDto.documentosRelacionados?`; `DteStrategy.requiereDocumentoRelacionado`, `DteStrategy.prefillDesdeOriginal`; `FacturaFormValues.documentoRelacionado?`.

- [ ] **Step 1:** En `types.ts` añade (junto a los demás DTOs):
```ts
export interface DocumentoRelacionadoDto {
  tipoDocumento: string // '03' para CCF
  tipoGeneracion: number // 1=Propio, 2=Externo
  numeroDocumento: string // codigoGeneracion del original
  fechaEmision: string // yyyy-MM-dd
}

export interface BuscarParaNcResultDto {
  id: number
  numeroControl: string
  codigoGeneracion: string
  tipoDte: string
  tipoDocumentoNombre: string
  fechaEmision: string
  receptorNombre: string
  receptorNumDocumento: string
  montoTotalOperacion: number
  totalPagar: number
  estadoHacienda: string
  saldoDisponible: number
  montoAcreditado: number
  nceCount: number
}

export interface DetalleItemParaNcDto {
  numItem: number
  tipoItem: number
  codigo?: string | null
  descripcion: string
  cantidad: number
  precioUnitario: number
  ventaGravada: number
  ventaExenta: number
  ventaNoSuj: number
  montoDescuento: number
  ivaItem: number
  unidadMedida?: number | null
  numeroDocumentoRelacionado?: string | null
}

export interface DetalleParaNcDto {
  id: number
  numeroControl: string
  codigoGeneracion: string
  tipoDte: string
  fechaEmision: string
  condicionOperacion: number
  receptorId?: number | null
  receptorNombre: string
  receptorNumDocumento: string
  receptorNit?: string | null
  receptorNrc?: string | null
  receptorCorreo?: string | null
  receptorTelefono?: string | null
  totalGravada: number
  totalExenta: number
  totalNoSuj: number
  subTotal: number
  totalIva: number
  montoTotalOperacion: number
  totalPagar: number
  saldoDisponible: number
  montoAcreditado: number
  nceCount: number
  items: DetalleItemParaNcDto[]
}
```
Y en `CreateFacturaDto` añade el campo opcional:
```ts
  documentosRelacionados?: DocumentoRelacionadoDto[]
```

- [ ] **Step 2:** En `dte/strategy.ts`:
- Añade a `DteStrategy` (junto a los flags existentes):
```ts
  requiereDocumentoRelacionado: boolean
  prefillDesdeOriginal: boolean
```
- Añade a `FacturaFormValues`:
```ts
  documentoRelacionado?: import('../types').DocumentoRelacionadoDto | null
```

- [ ] **Step 3:** Actualiza las estrategias existentes para los nuevos flags (build romperá si falta una propiedad). En `factura01.ts`, `ccf03.ts`, `fse14.ts` añade a cada objeto estrategia:
```ts
  requiereDocumentoRelacionado: false,
  prefillDesdeOriginal: false,
```

- [ ] **Step 4: `npm run build`** → PASS (tsc valida que todas las estrategias tienen los flags).

- [ ] **Step 5: `npx vitest run src/features/facturacion/`** → PASS (sin regresiones).

- [ ] **Step 6: Commit**
```bash
git add src/features/facturacion/types.ts src/features/facturacion/dte/strategy.ts src/features/facturacion/dte/factura01.ts src/features/facturacion/dte/ccf03.ts src/features/facturacion/dte/fse14.ts
git commit -m "feat(f5.3): tipos de documento relacionado + flags de estrategia NC/ND

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Estrategia NC (05)

**Files:**
- Create: `src/features/facturacion/dte/nc05.ts`
- Create: `src/features/facturacion/dte/nc05.test.ts`
- Modify: `src/features/facturacion/dte/registry.ts`

**Interfaces:**
- Consumes: `calcItemIvaNeto`/`calcResumenIvaNeto` (Task 1); `numeroALetras`; `DteStrategy`/`FormItem`/`FacturaFormValues` (strategy.ts); `ItemDocumentoDto`/`ResumenDto` (types.ts).
- Produces: `ncStrategy: DteStrategy`; registro de `'05'`.

- [ ] **Step 1: Test** `src/features/facturacion/dte/nc05.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { ncStrategy } from './nc05'

describe('ncStrategy (05) metadatos', () => {
  it('version 4, sin caja, requiere documento relacionado, contribuyente, prefill', () => {
    expect(ncStrategy.tipoDte).toBe('05')
    expect(ncStrategy.version).toBe(4)
    expect(ncStrategy.requiereCaja).toBe(false)
    expect(ncStrategy.descuentaStock).toBe(false)
    expect(ncStrategy.receptorPolicy).toBe('contribuyente')
    expect(ncStrategy.requiereDocumentoRelacionado).toBe(true)
    expect(ncStrategy.prefillDesdeOriginal).toBe(true)
  })
  it('buildItemDto: IVA neto con tributo ["20"] en gravado', () => {
    const item = { descripcion: 'X', cantidad: 2, precioUni: 50, uniMedida: 39, tipoItem: 1, tipoImpuesto: 1 as const }
    const dto = ncStrategy.buildItemDto(item, ncStrategy.calcItem(item), 1)
    expect(dto.ventaGravada).toBe(100)
    expect(dto.ivaItem).toBe(13)
    expect(dto.tributos).toEqual(['20'])
    expect(dto.precioIncluyeIva).toBe(false)
  })
  it('buildResumenDto: tributos + montoTotalOperacion', () => {
    const calc = [ncStrategy.calcItem({ cantidad: 2, precioUni: 50, tipoImpuesto: 1 })]
    const res = ncStrategy.calcResumen(calc)
    const dto = ncStrategy.buildResumenDto(res, { sucursalId: 1, cajaId: null, esConsumidorFinal: false, condicionOperacion: 1, receptorId: 7, items: [], pagos: [{ catFormaPagoId: 1, monto: 113 }] })
    expect(dto.montoTotalOperacion).toBe(113)
    expect(dto.totalPagar).toBe(113)
    expect(dto.tributos).toEqual([{ codigo: '20', descripcion: 'Impuesto al Valor Agregado 13%', valor: 13 }])
  })
})
```

- [ ] **Step 2: Ejecutar (falla)** — `npx vitest run src/features/facturacion/dte/nc05.test.ts` → FAIL.

- [ ] **Step 3: Crear `nc05.ts`** (estrategia idéntica en cálculo/DTO a CCF, con los flags NC):
```ts
import { numeroALetras } from '../calc/numero-letras'
import { calcItemIvaNeto, calcResumenIvaNeto } from '../calc/iva-neto'
import type { ItemCalculado, ResumenNumerico } from '../calc/types'
import type { ItemDocumentoDto, ResumenDto } from '../types'
import type { DteStrategy, FormItem, FacturaFormValues } from './strategy'

// NC (05): ajuste a la baja de un CCF. Mismo cálculo IVA-neto que el 03.
export const ncStrategy: DteStrategy = {
  tipoDte: '05',
  version: 4,
  etiqueta: 'Nota de Crédito',
  etiquetaCorta: 'NC',
  precioIncluyeIva: false,
  requiereCaja: false,
  descuentaStock: false,
  receptorPolicy: 'contribuyente',
  usaAgenteRetencion: false,
  requiereDocumentoRelacionado: true,
  prefillDesdeOriginal: true,
  calcItem: calcItemIvaNeto,
  calcResumen: (items: ItemCalculado[]): ResumenNumerico => calcResumenIvaNeto(items),
  buildItemDto(item: FormItem, calc: ItemCalculado, numItem: number): ItemDocumentoDto {
    return {
      numItem,
      tipoItem: item.tipoItem,
      cantidad: item.cantidad,
      codigo: item.codigo,
      uniMedida: item.uniMedida,
      descripcion: item.descripcion,
      precioUni: item.precioUni,
      montoDescuento: item.montoDescuento ?? 0,
      ventaGravada: calc.ventaGravada,
      ventaExenta: calc.ventaExenta,
      ventaNoSuj: calc.ventaNoSuj,
      ivaItem: calc.ivaItem,
      tributos: calc.ventaGravada > 0 ? ['20'] : null,
      productoId: item.productoId,
      precioIncluyeIva: false,
    }
  },
  buildResumenDto(resumen: ResumenNumerico, values: FacturaFormValues): ResumenDto {
    return {
      totalNoSuj: resumen.totalNoSuj,
      totalExenta: resumen.totalExenta,
      totalGravada: resumen.totalGravada,
      subTotal: resumen.subTotal,
      totalIva: resumen.totalIva,
      montoTotalOperacion: resumen.montoTotalOperacion,
      reteRenta: 0,
      tributos: resumen.totalIva > 0 ? [{ codigo: '20', descripcion: 'Impuesto al Valor Agregado 13%', valor: resumen.totalIva }] : undefined,
      totalPagar: resumen.totalPagar,
      totalLetras: numeroALetras(resumen.totalPagar),
      condicionOperacion: values.condicionOperacion,
      pagos: values.pagos.map((p) => ({ catFormaPagoId: p.catFormaPagoId, monto: p.monto })),
    }
  },
}
```

- [ ] **Step 4: Registrar '05'** en `registry.ts`: añade `import { ncStrategy } from './nc05'` y `'05': ncStrategy,` al objeto `dteRegistry`.

- [ ] **Step 5: Ejecutar** — `npx vitest run src/features/facturacion/dte/nc05.test.ts src/features/facturacion/dte/registry.test.ts` → PASS.

- [ ] **Step 6: `npm run build`** → PASS.

- [ ] **Step 7: Commit**
```bash
git add src/features/facturacion/dte/nc05.ts src/features/facturacion/dte/nc05.test.ts src/features/facturacion/dte/registry.ts
git commit -m "feat(f5.3): estrategia Nota de Crédito (05)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: Estrategia ND (06)

**Files:**
- Create: `src/features/facturacion/dte/nd06.ts`
- Create: `src/features/facturacion/dte/nd06.test.ts`
- Modify: `src/features/facturacion/dte/registry.ts`

**Interfaces:**
- Produces: `ndStrategy: DteStrategy`; registro de `'06'`. Idéntica a `ncStrategy` salvo `tipoDte:'06'`, `etiqueta:'Nota de Débito'`, `etiquetaCorta:'ND'`, `prefillDesdeOriginal:false`.

- [ ] **Step 1: Test** `src/features/facturacion/dte/nd06.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { ndStrategy } from './nd06'

describe('ndStrategy (06) metadatos', () => {
  it('version 4, requiere documento relacionado, SIN prefill', () => {
    expect(ndStrategy.tipoDte).toBe('06')
    expect(ndStrategy.version).toBe(4)
    expect(ndStrategy.requiereCaja).toBe(false)
    expect(ndStrategy.receptorPolicy).toBe('contribuyente')
    expect(ndStrategy.requiereDocumentoRelacionado).toBe(true)
    expect(ndStrategy.prefillDesdeOriginal).toBe(false)
  })
  it('buildResumenDto: IVA neto con tributos', () => {
    const calc = [ndStrategy.calcItem({ cantidad: 1, precioUni: 100, tipoImpuesto: 1 })]
    const res = ndStrategy.calcResumen(calc)
    const dto = ndStrategy.buildResumenDto(res, { sucursalId: 1, cajaId: null, esConsumidorFinal: false, condicionOperacion: 1, receptorId: 7, items: [], pagos: [{ catFormaPagoId: 1, monto: 113 }] })
    expect(dto.montoTotalOperacion).toBe(113)
    expect(dto.tributos).toEqual([{ codigo: '20', descripcion: 'Impuesto al Valor Agregado 13%', valor: 13 }])
  })
})
```

- [ ] **Step 2: Ejecutar (falla)** — `npx vitest run src/features/facturacion/dte/nd06.test.ts` → FAIL.

- [ ] **Step 3: Crear `nd06.ts`** — copia EXACTA de `nc05.ts` cambiando sólo: `tipoDte:'06'`, `etiqueta:'Nota de Débito'`, `etiquetaCorta:'ND'`, `prefillDesdeOriginal:false`, y renombrando el export a `ndStrategy`. Todo lo demás (cálculo, buildItemDto, buildResumenDto) idéntico:
```ts
import { numeroALetras } from '../calc/numero-letras'
import { calcItemIvaNeto, calcResumenIvaNeto } from '../calc/iva-neto'
import type { ItemCalculado, ResumenNumerico } from '../calc/types'
import type { ItemDocumentoDto, ResumenDto } from '../types'
import type { DteStrategy, FormItem, FacturaFormValues } from './strategy'

// ND (06): ajuste al alza de un CCF (cargos nuevos). Mismo cálculo IVA-neto que el 03.
export const ndStrategy: DteStrategy = {
  tipoDte: '06',
  version: 4,
  etiqueta: 'Nota de Débito',
  etiquetaCorta: 'ND',
  precioIncluyeIva: false,
  requiereCaja: false,
  descuentaStock: false,
  receptorPolicy: 'contribuyente',
  usaAgenteRetencion: false,
  requiereDocumentoRelacionado: true,
  prefillDesdeOriginal: false,
  calcItem: calcItemIvaNeto,
  calcResumen: (items: ItemCalculado[]): ResumenNumerico => calcResumenIvaNeto(items),
  buildItemDto(item: FormItem, calc: ItemCalculado, numItem: number): ItemDocumentoDto {
    return {
      numItem,
      tipoItem: item.tipoItem,
      cantidad: item.cantidad,
      codigo: item.codigo,
      uniMedida: item.uniMedida,
      descripcion: item.descripcion,
      precioUni: item.precioUni,
      montoDescuento: item.montoDescuento ?? 0,
      ventaGravada: calc.ventaGravada,
      ventaExenta: calc.ventaExenta,
      ventaNoSuj: calc.ventaNoSuj,
      ivaItem: calc.ivaItem,
      tributos: calc.ventaGravada > 0 ? ['20'] : null,
      productoId: item.productoId,
      precioIncluyeIva: false,
    }
  },
  buildResumenDto(resumen: ResumenNumerico, values: FacturaFormValues): ResumenDto {
    return {
      totalNoSuj: resumen.totalNoSuj,
      totalExenta: resumen.totalExenta,
      totalGravada: resumen.totalGravada,
      subTotal: resumen.subTotal,
      totalIva: resumen.totalIva,
      montoTotalOperacion: resumen.montoTotalOperacion,
      reteRenta: 0,
      tributos: resumen.totalIva > 0 ? [{ codigo: '20', descripcion: 'Impuesto al Valor Agregado 13%', valor: resumen.totalIva }] : undefined,
      totalPagar: resumen.totalPagar,
      totalLetras: numeroALetras(resumen.totalPagar),
      condicionOperacion: values.condicionOperacion,
      pagos: values.pagos.map((p) => ({ catFormaPagoId: p.catFormaPagoId, monto: p.monto })),
    }
  },
}
```

- [ ] **Step 4: Registrar '06'** en `registry.ts`: `import { ndStrategy } from './nd06'` y `'06': ndStrategy,`.

- [ ] **Step 5: Ejecutar** — `npx vitest run src/features/facturacion/dte/nd06.test.ts src/features/facturacion/dte/registry.test.ts` → PASS.

- [ ] **Step 6: `npm run build`** → PASS.

- [ ] **Step 7: Commit**
```bash
git add src/features/facturacion/dte/nd06.ts src/features/facturacion/dte/nd06.test.ts src/features/facturacion/dte/registry.ts
git commit -m "feat(f5.3): estrategia Nota de Débito (06)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: Mapper — incluir documentosRelacionados

**Files:**
- Modify: `src/features/facturacion/mappers.ts`

**Interfaces:**
- Consumes: `FacturaFormValues.documentoRelacionado` (Task 2); `CreateFacturaDto.documentosRelacionados` (Task 2).
- Produces: `buildCreateFacturaDto` que añade `documentosRelacionados` cuando `strategy.requiereDocumentoRelacionado` y hay `values.documentoRelacionado`.

- [ ] **Step 1: Test** — añade a `src/features/facturacion/mappers.test.ts` (o crea `nc05.mapper.test.ts`) un caso:
```ts
import { describe, it, expect } from 'vitest'
import { buildCreateFacturaDto, type FacturaFormValues } from './mappers'

const base: FacturaFormValues = {
  sucursalId: 1, cajaId: null, esConsumidorFinal: false, receptorId: 7, vendedorId: null, condicionOperacion: 1,
  documentoRelacionado: { tipoDocumento: '03', tipoGeneracion: 1, numeroDocumento: 'ABC-GUID', fechaEmision: '2026-06-20' },
  items: [{ descripcion: 'X', cantidad: 2, precioUni: 50, uniMedida: 39, tipoItem: 1, tipoImpuesto: 1 }],
  pagos: [{ catFormaPagoId: 1, monto: 113 }],
}

describe('mapper NC (05): documentosRelacionados', () => {
  it('incluye el documento relacionado y version 4', () => {
    const dto = buildCreateFacturaDto(base, { fecha: '2026-06-26', hora: '10:00:00' }, '05')
    expect(dto.identificacion.version).toBe(4)
    expect(dto.documentosRelacionados).toEqual([{ tipoDocumento: '03', tipoGeneracion: 1, numeroDocumento: 'ABC-GUID', fechaEmision: '2026-06-20' }])
    expect(dto.cajaId).toBeNull()
    expect(dto.receptorId).toBe(7)
  })
})
```

- [ ] **Step 2: Ejecutar (falla)** — `npx vitest run src/features/facturacion/mappers.test.ts` → FAIL (`documentosRelacionados` undefined).

- [ ] **Step 3: Modificar `buildCreateFacturaDto`** — en el objeto que retorna, tras `resumen:`, añade:
```ts
    documentosRelacionados:
      strategy.requiereDocumentoRelacionado && values.documentoRelacionado
        ? [values.documentoRelacionado]
        : undefined,
```

- [ ] **Step 4: Ejecutar** — `npx vitest run src/features/facturacion/mappers.test.ts` → PASS.

- [ ] **Step 5: `npm run build`** → PASS.

- [ ] **Step 6: Commit**
```bash
git add src/features/facturacion/mappers.ts src/features/facturacion/mappers.test.ts
git commit -m "feat(f5.3): mapper incluye documentosRelacionados para NC/ND

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: Schema — exigir documento relacionado (05/06)

**Files:**
- Modify: `src/features/facturacion/schemas.ts`

**Interfaces:**
- Consumes: `strategy.requiereDocumentoRelacionado`. El esquema gana el campo `documentoRelacionado` (opcional en el shape, exigido por `superRefine` cuando aplica).

- [ ] **Step 1: Test** — crea `src/features/facturacion/dte/nc05.mapper.test.ts` con la parte de schema:
```ts
import { describe, it, expect } from 'vitest'
import { buildFacturaSchema } from '../schemas'
import { getStrategy } from './registry'
import type { FacturaFormValues } from '../mappers'

const base: FacturaFormValues = {
  sucursalId: 1, cajaId: null, esConsumidorFinal: false, receptorId: 7, vendedorId: null, condicionOperacion: 1,
  documentoRelacionado: { tipoDocumento: '03', tipoGeneracion: 1, numeroDocumento: 'ABC', fechaEmision: '2026-06-20' },
  items: [{ descripcion: 'X', cantidad: 2, precioUni: 50, uniMedida: 39, tipoItem: 1, tipoImpuesto: 1 }],
  pagos: [{ catFormaPagoId: 1, monto: 113 }],
}

describe('schema NC (05)', () => {
  it('acepta con documento relacionado + receptor', () => {
    expect(buildFacturaSchema(getStrategy('05')).safeParse(base).success).toBe(true)
  })
  it('rechaza sin documento relacionado', () => {
    expect(buildFacturaSchema(getStrategy('05')).safeParse({ ...base, documentoRelacionado: null }).success).toBe(false)
  })
  it('rechaza sin receptor', () => {
    expect(buildFacturaSchema(getStrategy('05')).safeParse({ ...base, receptorId: null }).success).toBe(false)
  })
})
```

- [ ] **Step 2: Ejecutar (falla)** — `npx vitest run src/features/facturacion/dte/nc05.mapper.test.ts` → FAIL.

- [ ] **Step 3: Modificar `schemas.ts`:**
- Añade al `z.object({...})` (junto a los demás campos):
```ts
      documentoRelacionado: z
        .object({ tipoDocumento: z.string(), tipoGeneracion: z.number().int(), numeroDocumento: z.string().min(1), fechaEmision: z.string().min(1) })
        .nullable()
        .optional(),
```
- En el `superRefine`, tras el bloque de receptor, añade:
```ts
      // Documento relacionado: obligatorio para NC (05) / ND (06).
      if (strategy.requiereDocumentoRelacionado && !v.documentoRelacionado) {
        ctx.addIssue({ code: z.ZodIssueCode.custom, path: ['documentoRelacionado'], message: 'Seleccione el documento original (CCF) a referenciar' })
      }
```

- [ ] **Step 4: Ejecutar** — `npx vitest run src/features/facturacion/dte/nc05.mapper.test.ts` → PASS.

- [ ] **Step 5: `npm run build`** → PASS.

- [ ] **Step 6: Commit**
```bash
git add src/features/facturacion/schemas.ts src/features/facturacion/dte/nc05.mapper.test.ts
git commit -m "feat(f5.3): schema exige documento relacionado para NC/ND

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: API — buscar/detalle para NC y ND

**Files:**
- Modify: `src/features/facturacion/api.ts`

**Interfaces:**
- Consumes: `BuscarParaNcResultDto`, `DetalleParaNcDto` (Task 2); cliente `api` (axios, baseURL `…/api`).
- Produces: en `facturacionApi`: `buscarParaNc(search)`, `buscarParaNd(search)`, `detalleParaNc(id)`, `detalleParaNd(id)`.

- [ ] **Step 1:** En `api.ts`, dentro del objeto `facturacionApi`, añade (importa los tipos arriba):
```ts
  async buscarParaNc(search: string): Promise<BuscarParaNcResultDto[]> {
    const { data } = await api.get<BuscarParaNcResultDto[]>('/facturas/buscar-para-nc', { params: { search } })
    return data
  },
  async buscarParaNd(search: string): Promise<BuscarParaNcResultDto[]> {
    const { data } = await api.get<BuscarParaNcResultDto[]>('/facturas/buscar-para-nd', { params: { search } })
    return data
  },
  async detalleParaNc(id: number): Promise<DetalleParaNcDto> {
    const { data } = await api.get<DetalleParaNcDto>(`/facturas/${id}/detalle-para-nc`)
    return data
  },
  async detalleParaNd(id: number): Promise<DetalleParaNcDto> {
    const { data } = await api.get<DetalleParaNcDto>(`/facturas/${id}/detalle-para-nd`)
    return data
  },
```

- [ ] **Step 2: `npm run build`** → PASS (no hay test unitario de cliente axios; se cubre vía hooks/MSW en Task 8/9).

- [ ] **Step 3: Commit**
```bash
git add src/features/facturacion/api.ts
git commit -m "feat(f5.3): API buscar/detalle para NC y ND

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Hooks + handlers MSW

**Files:**
- Modify: `src/features/facturacion/hooks.ts`
- Modify: `src/test/msw/facturacion-handlers.ts`

**Interfaces:**
- Produces: `useBuscarParaNc()`/`useBuscarParaNd()` (devuelven función de búsqueda async para `AsyncSearchSelect`, o un hook de query por término); `useDetalleParaNc(id)`/`useDetalleParaNd(id)`. Para simplicidad y encajar con `AsyncSearchSelect` (que recibe `onSearch: (term)=>Promise<T[]>`), exponemos las funciones de búsqueda directas vía `facturacionApi` y un hook de detalle por id.

- [ ] **Step 1: Handlers MSW** — en `facturacion-handlers.ts` añade al array (antes del `]` final):
```ts
  http.get(`${base}/facturas/buscar-para-nc`, () => HttpResponse.json([
    { id: 8, numeroControl: 'DTE-03-...', codigoGeneracion: 'CCF-GUID-1', tipoDte: '03', tipoDocumentoNombre: 'CCF', fechaEmision: '2026-06-20', receptorNombre: 'Cliente Uno', receptorNumDocumento: '0614', montoTotalOperacion: 113, totalPagar: 113, estadoHacienda: 'PROCESADO', saldoDisponible: 113, montoAcreditado: 0, nceCount: 0 },
  ])),
  http.get(`${base}/facturas/buscar-para-nd`, () => HttpResponse.json([
    { id: 8, numeroControl: 'DTE-03-...', codigoGeneracion: 'CCF-GUID-1', tipoDte: '03', tipoDocumentoNombre: 'CCF', fechaEmision: '2026-06-20', receptorNombre: 'Cliente Uno', receptorNumDocumento: '0614', montoTotalOperacion: 113, totalPagar: 113, estadoHacienda: 'PROCESADO', saldoDisponible: 113, montoAcreditado: 0, nceCount: 0 },
  ])),
  http.get(`${base}/facturas/:id/detalle-para-nc`, () => HttpResponse.json({
    id: 8, numeroControl: 'DTE-03-...', codigoGeneracion: 'CCF-GUID-1', tipoDte: '03', fechaEmision: '2026-06-20', condicionOperacion: 1,
    receptorId: 7, receptorNombre: 'Cliente Uno', receptorNumDocumento: '0614', receptorNit: '0614', receptorNrc: '123-4', receptorCorreo: 'c@x.com', receptorTelefono: '2222',
    totalGravada: 100, totalExenta: 0, totalNoSuj: 0, subTotal: 100, totalIva: 13, montoTotalOperacion: 113, totalPagar: 113,
    saldoDisponible: 113, montoAcreditado: 0, nceCount: 0,
    items: [{ numItem: 1, tipoItem: 1, codigo: 'P001', descripcion: 'Producto A', cantidad: 2, precioUnitario: 50, ventaGravada: 100, ventaExenta: 0, ventaNoSuj: 0, montoDescuento: 0, ivaItem: 13, unidadMedida: 39, numeroDocumentoRelacionado: null }],
  })),
  http.get(`${base}/facturas/:id/detalle-para-nd`, () => HttpResponse.json({
    id: 8, numeroControl: 'DTE-03-...', codigoGeneracion: 'CCF-GUID-1', tipoDte: '03', fechaEmision: '2026-06-20', condicionOperacion: 1,
    receptorId: 7, receptorNombre: 'Cliente Uno', receptorNumDocumento: '0614', receptorNit: '0614', receptorNrc: '123-4', receptorCorreo: 'c@x.com', receptorTelefono: '2222',
    totalGravada: 100, totalExenta: 0, totalNoSuj: 0, subTotal: 100, totalIva: 13, montoTotalOperacion: 113, totalPagar: 113,
    saldoDisponible: 113, montoAcreditado: 0, nceCount: 0, items: [],
  })),
```

- [ ] **Step 2: Hooks** — en `hooks.ts` añade:
```ts
export function useDetalleParaNc(id: number | null, enabled: boolean) {
  return useQuery({ queryKey: ['detalle-para-nc', id], queryFn: () => facturacionApi.detalleParaNc(id!), enabled: enabled && Number.isFinite(id) })
}
export function useDetalleParaNd(id: number | null, enabled: boolean) {
  return useQuery({ queryKey: ['detalle-para-nd', id], queryFn: () => facturacionApi.detalleParaNd(id!), enabled: enabled && Number.isFinite(id) })
}
```
(La búsqueda se consume directamente vía `facturacionApi.buscarParaNc/Nd` en `DocumentoOriginalSection`, igual que `receptoresApi.search` en `QuickCreateSelect`.)

- [ ] **Step 3: `npm run build`** → PASS.

- [ ] **Step 4: Commit**
```bash
git add src/features/facturacion/hooks.ts src/test/msw/facturacion-handlers.ts
git commit -m "feat(f5.3): hooks de detalle + handlers MSW para NC/ND

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: DocumentoOriginalSection (buscar + seleccionar + precargar)

**Files:**
- Create: `src/features/facturacion/components/DocumentoOriginalSection.tsx`
- Create: `src/features/facturacion/components/DocumentoOriginalSection.test.tsx`

**Interfaces:**
- Consumes: `AsyncSearchSelect`; `facturacionApi.buscarParaNc/Nd`, `facturacionApi.detalleParaNc/Nd`; tipos `BuscarParaNcResultDto`/`DetalleParaNcDto`/`DetalleItemParaNcDto`/`DocumentoRelacionadoDto`; `FormItem`, `ReceptorListItem`.
- Produces: `DocumentoOriginalSection({ tipoDte, prefill, onSeleccion })` donde `onSeleccion({ documentoRelacionado, receptor, items })` entrega el documento relacionado armado, el receptor heredado (como `ReceptorListItem`) y los ítems precargados (`FormItem[]`, vacío si `prefill` es false).

- [ ] **Step 1: Test** `DocumentoOriginalSection.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { type ReactElement } from 'react'
import { DocumentoOriginalSection } from './DocumentoOriginalSection'

function wrap(ui: ReactElement) {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return render(<QueryClientProvider client={qc}>{ui}</QueryClientProvider>)
}

describe('DocumentoOriginalSection', () => {
  it('NC: al elegir el CCF entrega documento relacionado, receptor e ítems precargados', async () => {
    const user = userEvent.setup()
    const onSeleccion = vi.fn()
    wrap(<DocumentoOriginalSection tipoDte="05" prefill={true} onSeleccion={onSeleccion} />)
    await user.type(screen.getByRole('combobox'), 'cliente')
    const opcion = await screen.findByText(/CCF-GUID-1|Cliente Uno|DTE-03/i)
    await user.click(opcion)
    await waitFor(() => expect(onSeleccion).toHaveBeenCalled())
    const arg = onSeleccion.mock.calls.at(-1)![0]
    expect(arg.documentoRelacionado).toEqual({ tipoDocumento: '03', tipoGeneracion: 1, numeroDocumento: 'CCF-GUID-1', fechaEmision: '2026-06-20' })
    expect(arg.receptor.id).toBe(7)
    expect(arg.items).toHaveLength(1)
    expect(arg.items[0].descripcion).toBe('Producto A')
  })
  it('ND: prefill=false entrega items vacíos', async () => {
    const user = userEvent.setup()
    const onSeleccion = vi.fn()
    wrap(<DocumentoOriginalSection tipoDte="06" prefill={false} onSeleccion={onSeleccion} />)
    await user.type(screen.getByRole('combobox'), 'cliente')
    await user.click(await screen.findByText(/CCF-GUID-1|Cliente Uno|DTE-03/i))
    await waitFor(() => expect(onSeleccion).toHaveBeenCalled())
    expect(onSeleccion.mock.calls.at(-1)![0].items).toHaveLength(0)
  })
})
```

- [ ] **Step 2: Ejecutar (falla)** — `npx vitest run src/features/facturacion/components/DocumentoOriginalSection.test.tsx` → FAIL.

- [ ] **Step 3: Implementar `DocumentoOriginalSection.tsx`:**
```tsx
import { useState } from 'react'
import { FormField } from '@/design-system'
import { AsyncSearchSelect } from './AsyncSearchSelect'
import { facturacionApi } from '../api'
import { TIPO_ITEM } from '../catalogos'
import type { BuscarParaNcResultDto, DetalleItemParaNcDto, DocumentoRelacionadoDto, ReceptorListItem, TipoDte } from '../types'
import type { FormItem } from '../mappers'

interface SeleccionPayload {
  documentoRelacionado: DocumentoRelacionadoDto
  receptor: ReceptorListItem
  items: FormItem[]
}
interface Props {
  tipoDte: TipoDte
  prefill: boolean
  onSeleccion: (p: SeleccionPayload) => void
}

function tipoImpuestoDeItem(it: DetalleItemParaNcDto): 1 | 2 | 3 {
  if (it.ventaGravada > 0) return 1
  if (it.ventaExenta > 0) return 2
  return 3
}
function aFormItem(it: DetalleItemParaNcDto): FormItem {
  return {
    codigo: it.codigo ?? undefined,
    descripcion: it.descripcion,
    cantidad: it.cantidad,
    precioUni: it.precioUnitario,
    montoDescuento: it.montoDescuento || undefined,
    uniMedida: it.unidadMedida ?? 0,
    tipoItem: it.tipoItem,
    tipoImpuesto: tipoImpuestoDeItem(it),
  }
}

export function DocumentoOriginalSection({ tipoDte, prefill, onSeleccion }: Props) {
  const [elegido, setElegido] = useState<BuscarParaNcResultDto | null>(null)
  const buscar = (term: string) => (tipoDte === '05' ? facturacionApi.buscarParaNc(term) : facturacionApi.buscarParaNd(term))

  const seleccionar = async (r: BuscarParaNcResultDto) => {
    setElegido(r)
    const detalle = tipoDte === '05' ? await facturacionApi.detalleParaNc(r.id) : await facturacionApi.detalleParaNd(r.id)
    onSeleccion({
      documentoRelacionado: { tipoDocumento: '03', tipoGeneracion: 1, numeroDocumento: r.codigoGeneracion, fechaEmision: r.fechaEmision.slice(0, 10) },
      receptor: {
        id: detalle.receptorId ?? 0,
        tipoDocumentoNombre: '',
        numeroDocumento: detalle.receptorNumDocumento,
        nombreRazonSocial: detalle.receptorNombre,
        correoElectronico: detalle.receptorCorreo ?? '',
        nrc: detalle.receptorNrc ?? '',
      },
      items: prefill ? detalle.items.map(aFormItem) : [],
    })
  }

  return (
    <section className="space-y-3">
      <h2 className="flex items-center gap-2 font-mono text-[11px] uppercase tracking-[0.14em] text-slate/80"><span className="h-1.5 w-1.5 rounded-full bg-oro" />Documento original (CCF)</h2>
      <FormField label="Buscar CCF a referenciar" htmlFor="doc-original">
        <AsyncSearchSelect<BuscarParaNcResultDto>
          onSearch={buscar}
          getLabel={(r) => `${r.numeroControl} · ${r.receptorNombre} · $${r.totalPagar.toFixed(2)} · saldo $${r.saldoDisponible.toFixed(2)}`}
          onSelect={seleccionar}
          placeholder="Buscar por nº de control, receptor o código"
        />
      </FormField>
      {elegido && (
        <div className="rounded-md border border-hairline px-3 py-2 text-sm">
          <div className="font-medium">{elegido.numeroControl}</div>
          <div className="text-slate">{elegido.receptorNombre} · {elegido.receptorNumDocumento}</div>
          <div className="text-slate">Total ${elegido.totalPagar.toFixed(2)} · Saldo disponible ${elegido.saldoDisponible.toFixed(2)}</div>
        </div>
      )}
    </section>
  )
}
```
Nota: `AsyncSearchSelect` usa `getLabel` para el desplegable; el test localiza la opción por texto (numeroControl/receptor). Si el render del desplegable difiere, ajusta el selector del test a cómo `AsyncSearchSelect` muestra cada opción (revisar su JSX), sin cambiar la lógica del componente.

- [ ] **Step 4: Ejecutar** — `npx vitest run src/features/facturacion/components/DocumentoOriginalSection.test.tsx` → PASS.

- [ ] **Step 5: `npm run build`** → PASS.

- [ ] **Step 6: Commit**
```bash
git add src/features/facturacion/components/DocumentoOriginalSection.tsx src/features/facturacion/components/DocumentoOriginalSection.test.tsx
git commit -m "feat(f5.3): DocumentoOriginalSection (buscar CCF + precarga NC)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 10: Cablear EmitirDtePage + ocultar receptor en DatosGeneralesSection

**Files:**
- Modify: `src/features/facturacion/pages/EmitirDtePage.tsx`
- Modify: `src/features/facturacion/components/DatosGeneralesSection.tsx`

**Interfaces:**
- Consumes: `DocumentoOriginalSection` (Task 9); `strategy.requiereDocumentoRelacionado`/`prefillDesdeOriginal`; `FacturaFormValues.documentoRelacionado`.

- [ ] **Step 1:** En `DatosGeneralesSection.tsx`, envuelve el bloque del receptor (el `<FormField label={receptorLabel} …>` completo, ternario de 3 vías de F5.2) en una condición para ocultarlo cuando el documento se hereda del original:
```tsx
      {!p.strategy.requiereDocumentoRelacionado && (
        <FormField label={receptorLabel} htmlFor="receptor">
          {/* …contenido existente del receptor sin cambios… */}
        </FormField>
      )}
```
(El bloque previo `receptorOpcional && (… esConsumidorFinal …)` también va condicionado a `!p.strategy.requiereDocumentoRelacionado`.)

- [ ] **Step 2:** En `EmitirDtePage.tsx`:
- Importa el componente: `import { DocumentoOriginalSection } from '../components/DocumentoOriginalSection'` y el tipo `DocumentoRelacionadoDto` desde `../types`.
- Añade estado: `const [documentoRelacionado, setDocumentoRelacionado] = useState<DocumentoRelacionadoDto | null>(null)`.
- Añade `documentoRelacionado` al objeto `values`:
```ts
  const values: FacturaFormValues = {
    sucursalId, cajaId, esConsumidorFinal, receptorId: receptor?.id ?? null, vendedorId, condicionOperacion, esAgenteRetencion, items, pagos, documentoRelacionado,
  }
```
- Renderiza la sección antes de `DatosGeneralesSection` cuando aplica:
```tsx
        {strategy.requiereDocumentoRelacionado && (
          <DocumentoOriginalSection
            tipoDte={strategy.tipoDte}
            prefill={strategy.prefillDesdeOriginal}
            onSeleccion={({ documentoRelacionado, receptor, items }) => {
              setDocumentoRelacionado(documentoRelacionado)
              setReceptor(receptor)
              setItems(items)
            }}
          />
        )}
```

- [ ] **Step 3: `npm run build`** → PASS.

- [ ] **Step 4:** Verifica que el test de página existente (`EmitirDtePage.test.tsx`, flujo 01) sigue verde: `npx vitest run src/features/facturacion/pages/EmitirDtePage.test.tsx` → PASS.

- [ ] **Step 5: Commit**
```bash
git add src/features/facturacion/pages/EmitirDtePage.tsx src/features/facturacion/components/DatosGeneralesSection.tsx
git commit -m "feat(f5.3): cablear documento original en la página de emisión (NC/ND)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 11: TotalesPanel + VistaPreviaDte para 05/06

**Files:**
- Modify: `src/features/facturacion/components/TotalesPanel.tsx`
- Modify: `src/features/facturacion/components/VistaPreviaDte.tsx`

**Interfaces:**
- 05/06 se muestran como el 03 (IVA neto). `VistaPreviaDte` además muestra el documento relacionado si está presente en el DTO.

- [ ] **Step 1: TotalesPanel** — en la rama de filas, trata 05/06 igual que 03. Cambia la condición `tipoDte === '03'` por incluir notas:
```tsx
        ) : tipoDte === '03' || tipoDte === '05' || tipoDte === '06' ? (
```
(mismo bloque de filas que el 03: gravado + IVA 13% neto + total).

- [ ] **Step 2: VistaPreviaDte** — añade el flag y, si el DTO trae documento relacionado, muéstralo. Tras `const esCcf = tipoDte === '03'` añade:
```tsx
  const esNota = tipoDte === '05' || tipoDte === '06'
```
Usa `esCcf || esNota` donde hoy se decide el tratamiento IVA-neto del CCF (etiqueta de IVA y montoTotalOperacion). Y antes del bloque de totales, si hay documento relacionado:
```tsx
      {dto.documentosRelacionados?.[0] && (
        <p className="text-xs text-slate">Documento relacionado: {dto.documentosRelacionados[0].tipoDocumento} · {dto.documentosRelacionados[0].numeroDocumento}</p>
      )}
```

- [ ] **Step 3:** Verifica que no rompe la paridad 01/03/14: `npx vitest run src/features/facturacion/components/TotalesPanel.test.tsx src/features/facturacion/components/VistaPreviaDte.test.tsx` → PASS.

- [ ] **Step 4: `npm run build`** → PASS.

- [ ] **Step 5: Commit**
```bash
git add src/features/facturacion/components/TotalesPanel.tsx src/features/facturacion/components/VistaPreviaDte.tsx
git commit -m "feat(f5.3): TotalesPanel y VistaPreviaDte soportan NC/ND

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 12: Test e2e del DTO — NC (05) end-to-end + GATE

**Files:**
- Modify/expand: `src/features/facturacion/dte/nc05.mapper.test.ts`

- [ ] **Step 1:** Añade a `nc05.mapper.test.ts` un caso de DTO completo:
```ts
import { buildCreateFacturaDto } from '../mappers'
// (base ya definido en el archivo)
it('arma el DTO de NC con tributos, documento relacionado y version 4', () => {
  const dto = buildCreateFacturaDto(base, { fecha: '2026-06-26', hora: '10:00:00' }, '05')
  expect(dto.identificacion.tipoDte).toBe('05')
  expect(dto.identificacion.version).toBe(4)
  expect(dto.cajaId).toBeNull()
  expect(dto.cuerpoDocumento[0].tributos).toEqual(['20'])
  expect(dto.cuerpoDocumento[0].ivaItem).toBe(13)
  expect(dto.resumen.tributos).toEqual([{ codigo: '20', descripcion: 'Impuesto al Valor Agregado 13%', valor: 13 }])
  expect(dto.resumen.montoTotalOperacion).toBe(113)
  expect(dto.documentosRelacionados).toEqual([{ tipoDocumento: '03', tipoGeneracion: 1, numeroDocumento: 'ABC', fechaEmision: '2026-06-20' }])
})
```

- [ ] **Step 2: Ejecutar** — `npx vitest run src/features/facturacion/dte/nc05.mapper.test.ts` → PASS.

- [ ] **Step 3: GATE** — `npm run test` → PASS; `npm run build` → PASS; `npm run lint` → PASS.

- [ ] **Step 4: Commit**
```bash
git add src/features/facturacion/dte/nc05.mapper.test.ts
git commit -m "test(f5.3): DTO de NC (05) end-to-end

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 13: Test e2e del DTO — ND (06)

**Files:**
- Create: `src/features/facturacion/dte/nd06.mapper.test.ts`

- [ ] **Step 1:** Crea `nd06.mapper.test.ts` (cargo nuevo manual; mismo cálculo IVA neto):
```ts
import { describe, it, expect } from 'vitest'
import { buildCreateFacturaDto, type FacturaFormValues } from '../mappers'
import { buildFacturaSchema } from '../schemas'
import { getStrategy } from './registry'

const base: FacturaFormValues = {
  sucursalId: 1, cajaId: null, esConsumidorFinal: false, receptorId: 7, vendedorId: null, condicionOperacion: 1,
  documentoRelacionado: { tipoDocumento: '03', tipoGeneracion: 1, numeroDocumento: 'ABC', fechaEmision: '2026-06-20' },
  items: [{ descripcion: 'Interés por mora', cantidad: 1, precioUni: 100, uniMedida: 39, tipoItem: 2, tipoImpuesto: 1 }],
  pagos: [{ catFormaPagoId: 1, monto: 113 }],
}

describe('ND (06): mapper + schema', () => {
  it('arma el DTO con version 4, tributos y documento relacionado', () => {
    const dto = buildCreateFacturaDto(base, { fecha: '2026-06-26', hora: '10:00:00' }, '06')
    expect(dto.identificacion.tipoDte).toBe('06')
    expect(dto.identificacion.version).toBe(4)
    expect(dto.cuerpoDocumento[0].tributos).toEqual(['20'])
    expect(dto.resumen.montoTotalOperacion).toBe(113)
    expect(dto.documentosRelacionados?.[0].numeroDocumento).toBe('ABC')
  })
  it('schema rechaza sin documento relacionado', () => {
    expect(buildFacturaSchema(getStrategy('06')).safeParse({ ...base, documentoRelacionado: null }).success).toBe(false)
  })
})
```

- [ ] **Step 2: Ejecutar** — `npx vitest run src/features/facturacion/dte/nd06.mapper.test.ts` → PASS.

- [ ] **Step 3: GATE F5.3** — `npm run test` → PASS; `npm run build` → PASS; `npm run lint` → PASS.

- [ ] **Step 4: Commit**
```bash
git add src/features/facturacion/dte/nd06.mapper.test.ts
git commit -m "test(f5.3): DTO de ND (06) end-to-end

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 14: e2e contra Docker — NC (controlador)

**Nota:** esta tarea la ejecuta el controlador (no un subagente), contra el stack Docker vivo, replicando la forma exacta del frontend.

- [ ] **Step 1:** Asegurar stack arriba (`docker compose up -d` desde `FraFactu-Backend`) y login `admin@pruebas.local`/`Pruebas#2026`.
- [ ] **Step 2:** Emitir un **CCF (03)** de prueba (forma de F5.2: receptor contribuyente con distrito, caja, tributos) → capturar su `codigoGeneracion` y `fechaEmision`.
- [ ] **Step 3:** `GET /api/facturas/buscar-para-nc?search=<algo>` → localizar el CCF; `GET /api/facturas/{id}/detalle-para-nc` → ver ítems/saldo.
- [ ] **Step 4:** Construir el DTO de **NC (05)** con la forma del frontend: `documentosRelacionados:[{tipoDocumento:'03',tipoGeneracion:1,numeroDocumento:<codGenCCF>,fechaEmision:<fecha>}]`, receptor heredado (`receptorId`), ítems (parcial), tributos `['20']` + `resumen.tributos`, `version 4`, sin caja. `POST /api/facturas/guardar-pendiente`.
- [ ] **Step 5:** Verificar **HTTP 201** y estado `PENDIENTE_ENVIO`. Registrar en el ledger cualquier divergencia de contrato (campos extra que MH exija para 05). Si rechaza, ajustar estrategia/mapper/schema y re-validar.

---

## Task 15: e2e ND + revisión final de rama + memoria (controlador)

- [ ] **Step 1:** Construir y emitir una **ND (06)** con cargo nuevo (ítem manual) referenciando el mismo CCF → `POST guardar-pendiente` → **201 PENDIENTE_ENVIO**.
- [ ] **Step 2:** GATE final: `npm run test` + `npm run build` + `npm run lint` verdes.
- [ ] **Step 3:** Revisión whole-branch (superpowers:requesting-code-review) sobre el diff completo de la rama.
- [ ] **Step 4:** Actualizar `docs/operaciones/semilla-emisor-pruebas.md` (sección NC/ND) y la memoria del proyecto (`frafactu-estado-f5`).
- [ ] **Step 5:** finishing-a-development-branch: push + PR por web hacia `development` (preguntar rama al usuario).

---

## Self-Review (autor del plan)

- **Cobertura del spec:** estrategias NC/ND (T3/T4), cálculo compartido (T1), documento relacionado en tipos/mapper/schema (T2/T5/T6), API/hooks/MSW (T7/T8), UI buscar+precarga (T9/T10), totales/preview (T11), tests DTO 05/06 (T12/T13), e2e Docker (T14/T15). Punto de entrada = tarjetas en landing (automático vía registro en T3/T4). Receptor heredado y oculto (T9/T10). Saldo mostrado como guía (T9). ✓
- **Sin placeholders:** cada paso de código trae el código real. El único "ajuste según render" (selector del test en T9) está acotado a localizar la opción en `AsyncSearchSelect`, no a inventar lógica.
- **Consistencia de tipos:** `DocumentoRelacionadoDto`/`DetalleParaNcDto`/`DetalleItemParaNcDto` definidos en T2 y consumidos con las mismas firmas en T7/T8/T9; `requiereDocumentoRelacionado`/`prefillDesdeOriginal` definidos en T2 y usados en T3/T4/T6/T9/T10; `FacturaFormValues.documentoRelacionado` (T2) consumido en T5/T6/T10; `ncStrategy`/`ndStrategy` (T3/T4) registrados y usados por mapper/schema/paneles.
- **Riesgo abierto (a confirmar en e2e T14):** campos que MH pueda exigir extra para 05/06 (p.ej. `tipoOperacion`, ajuste de saldo). El patrón F5.1b/F5.2 cubre el ajuste si surge.
